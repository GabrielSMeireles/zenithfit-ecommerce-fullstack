{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "msedge",
            "request": "launch",
            "name": "Rodar Arquivo Atual",
            "file": "${file}"
        }
    ]
}